import React, {PureComponent} from "react";
import propTypes from "prop-types";

class CreateStore extends PureComponent{
  render(){
    return(
      <div>create store</div>
    );
  }
}

export default CreateStore;